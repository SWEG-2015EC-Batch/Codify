#include <iostream>

using namespace std;

int main()
{
   int sum=0, temp[8],i;
   for(i=0;i<8;i++){
    cout<<"enter value "<<i+1<<": ";
    cin>>temp[i];
    sum+=temp[i];

   }
 cout<<"sum: "<<sum<<endl;
  cout<<"avereage: "<<static_cast<float>(sum)/8;
    return 0;
}

